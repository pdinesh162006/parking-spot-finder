{
  "name": "parkease-root",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:frontend": "npm run build --workspace=frontend",
    "serve": "npm run build:frontend && npm run build --workspace=backend && set PORT=5001&& node backend/dist/index.js",
    "build": "npx prisma generate --schema=backend/prisma/schema.prisma && npm run build:frontend && npm run build --workspace=backend",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
